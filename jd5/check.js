/* eslint-disable */
const
    path = require('path'),
    fs = require('fs'),
    fsp = fs.promises;

async function check(sandbox, config) {

    return [status, score, message];
}

module.exports = check;
